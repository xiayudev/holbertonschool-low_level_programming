#include <stdio.h>

/**
 * main - Entry point
 *
 * Print a phrase using printf() function
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char phrase[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", phrase);

	return (0);
}
