#include <stdio.h>

/**
 * main - Entry point
 *
 * Print a phrase using puts() function
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char phrase[] = "\"Programming is like building a multilingual puzzle";

	puts(phrase);

	return (0);
}
